const routes = {
  HOME: "/",
  DASHBOARD: "/dashboard",
  LOGIN: "/login",
  REQUEST_ACCESS: "/request-access",
  PROD_DATA_EXPORTER: "/dashboard/prod-data-exporter",
};

export default routes;
export type RouteNames = typeof routes;
